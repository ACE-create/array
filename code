#include<iostream>
using namespace std;
int main()
{
    int arr[10];
    for(int i=0;i<10;i++)
      {
        cin>>arr[i];
      }
    int num;
    int count=1;
    for(int i=0;i<10;i++)
    {
        for(int j=0;j<10;j++)
        {
            if(arr[i]!=arr[j])
            {
                count=0;
            }
            else
            count=1;

        }
        if(count==0)
        {
            num=arr[i];
        }
    }
    cout<<num;
    
}
